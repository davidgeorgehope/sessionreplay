{
  "name": "session-replay",
  "version": "0.0.1",
  "private": true,
  "description": "AI-native session understanding - semantic events instead of DOM recording",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "demo": "node examples/demo-app/start.js",
    "demo:load": "node examples/demo-app/start.js --load",
    "demo:serve": "node examples/demo-app/server.js"
  },
  "keywords": [
    "session-replay",
    "opentelemetry",
    "observability",
    "user-experience",
    "frustration-detection"
  ],
  "author": "David Hope",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "dotenv": "^17.2.3"
  }
}
